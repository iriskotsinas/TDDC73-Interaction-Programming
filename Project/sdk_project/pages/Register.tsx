import React from 'react';
import RegisterUser from '../components/RegisterUser';

const Register = ({navigation}: any) => {
  return <RegisterUser navigation={navigation} />;
};

export default Register;
